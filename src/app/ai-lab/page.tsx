import { PageShell } from "@/components/ui";
import { AILab } from "@/components/ai-lab";
export default function Page(){return <PageShell eyebrow="AI intelligence lab" title="Structured diaspora profile classification" subtitle="OpenAI Structured Outputs are used when an API key is configured; otherwise the MVP returns a deterministic schema-compatible demo classification."><AILab /></PageShell>}
