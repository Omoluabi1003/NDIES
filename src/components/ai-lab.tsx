"use client";
import { useState } from "react";
import { Card } from "./ui";
export function AILab(){const [rawInput,setRawInput]=useState("Nigerian cardiologist leading digital health innovation at a major teaching hospital, active in professional associations and interested in mentoring clinicians in Lagos.");const [result,setResult]=useState<Record<string,unknown>|null>(null);const [loading,setLoading]=useState(false);async function classify(){setLoading(true);const res=await fetch('/api/ai/classify-profile',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({rawInput})});const json=await res.json();setResult(json.data);setLoading(false);}return <div className="grid gap-6 lg:grid-cols-2"><Card><h2 className="text-2xl font-semibold">Analyst input</h2><p className="mt-2 text-slate-400">Paste public professional profile text. The API returns strict JSON and logs the classification.</p><textarea className="mt-5 min-h-[260px] w-full" value={rawInput} onChange={e=>setRawInput(e.target.value)}/><button onClick={classify} className="mt-4 rounded-full bg-[#d6a73a] px-6 py-3 font-semibold text-[#071426]">{loading?'Classifying...':'Classify profile'}</button></Card><Card><h2 className="text-2xl font-semibold">Structured JSON output</h2><pre className="mt-5 max-h-[520px] overflow-auto rounded-2xl bg-[#020814] p-5 text-sm text-[#8fd4ff]">{result?JSON.stringify(result,null,2):'Run a classification to view schema-compliant output.'}</pre></Card></div>}
