import type { DiasporaProfile } from "@/lib/types";
import { Card, Score } from "./ui";
export function ProfileCard({p}:{p:DiasporaProfile}){return <Card><div className="flex flex-wrap items-start justify-between gap-3"><div><h3 className="text-xl font-semibold">{p.fullName}</h3><p className="text-sm text-slate-400">{p.professionTitle} · {p.organization}</p><p className="mt-1 text-sm text-[#8fd4ff]">{p.city}, {p.country} · {p.sector}</p></div><span className="badge">{p.engagementCategory}</span></div><div className="mt-4 flex flex-wrap gap-2">{p.skills.map(s=><span key={s} className="rounded-full bg-white/8 px-3 py-1 text-xs text-slate-300">{s}</span>)}</div><div className="mt-5 grid gap-3"><Score value={p.influenceScore}/><Score value={p.investmentCapacityScore}/><Score value={p.strategicValueIndex}/></div></Card>}
