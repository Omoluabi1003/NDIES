# Server-only secrets. Do not prefix with NEXT_PUBLIC_ and do not expose them to client components.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB?schema=public"
OPENAI_API_KEY="sk-REPLACE_ME"
OPENAI_MODEL="gpt-4o-mini"

# Optional: explicitly document runtime mode for local debugging.
NODE_ENV="development"
